 <ul class="list-2">
    @foreach (var item in ViewBag.Services){
    <li>
      <a href='@Url.FrontUrl().PageUrl("Hizmetlerimiz/Detay",new { UserKey = item.UserKey})'>@item.Name</a>
    </li>
    }
</ul>