 <br />
<h5 class="unreset" style="font-size:15pt">Diğer Hizmetlerimiz</h5>
<div class="line-hor p2 unreset"/>
<ul class="list-1 p3 unreset">
    @foreach (var item in ViewBag.Services){
    <li>
        <a class='title' href='@Url.FrontUrl().PageUrl("Hizmetlerimiz/Detay",new { UserKey = item.UserKey})'>@item.Name</a>
    </li>
    }
</ul>
<div style="height:10px; float:right"></div>