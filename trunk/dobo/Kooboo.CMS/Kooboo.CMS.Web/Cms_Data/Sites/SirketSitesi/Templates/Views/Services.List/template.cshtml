 <br />
<div style="width:900px; margin:5px">
    @foreach (var item in ViewBag.Services){
  <div style="float: left; width: 400px; padding-bottom:20px">
    <div style="padding-right:20px; float: left; height:120">
            @if(item.Image != null) {<img src="@Url.FrontUrl().ResizeImageUrl(item.Image,100,100)"></img>}
  </div>
  <div style="width:380px;">
            <a class='title' href='@Url.FrontUrl().PageUrl("Anasayfa/Hizmetlerimiz/Detay",new { UserKey = item.UserKey})'>
                @item.Name</a><br />
            @Html.Raw(item.Summary)
        </div>
    </div>
    }
</div>
<div class="pager">
    @Html.FrontHtml().Pager(ViewBag.Services)
<div>
